[pytest]
# 该仓库包含“参考项目”目录，仅用于对照实现，不属于本项目测试范围。
# 若不排除，会导致 `pytest` 默认递归收集时报错（依赖缺失等）。
norecursedirs =
    参考项目
    AI-Codereview-Gitlab
    .git
    .audit_cache
    .audit_data
    .claude
    .fortify_migration
    node_modules
    frontend

addopts = -ra